# CONTEXT

브라우저에서 마주치는 base64 인코딩 텍스트를 자동으로 찾아내어, 원문을 보존한 채
읽을 수 있는 형태로 함께 보여주는 유저스크립트.

## 용어

### 원문 토큰 (source token)
페이지에 원래부터 존재하던 base64 문자열. 우리가 만들어낸 것이 아니며, **절대 지우거나
바꾸지 않는다.** 사용자가 디코딩 결과를 원문과 대조해 검증할 수 있어야 하기 때문이다.

### 디코딩 블록 (decoded block)
원문 토큰을 해석한 결과를 담아 새로 삽입하는 표시 영역. 페이지에 없던 것을 우리가
추가하는 것이므로, 주변 사이트의 스타일과 격리된다.

### 삽입 기법 (insertion technique)
디코딩 블록을 문서에 넣는 방법. 원문 토큰의 보존 여부와는 **별개의 축**이다.
원문을 남기면서도 문서를 파괴하는 삽입이 가능하고, 그 반대도 가능하다.

### 기준점 (anchor point)
디코딩 블록이 놓이는 위치. 원문 토큰이 속한 가장 가까운 블록 요소의 바로 아래를 말한다.
"토큰 옆"이 아니라 "문단 밑"이다.

### 앵커 경로 (anchored path)
디코딩 결과가 무엇일지 **미리 알고 찾는** 탐지 경로. 찾는 대상이 확정되어 있으므로
오탐이 원리적으로 발생하지 않는다. 결과를 검증할 필요가 없어 자동으로 펼친다.

### 앵커 (anchor)
앵커 경로가 찾는 확정적 표식. 알려진 평문(예: URL 스킴)을 인코딩했을 때 반드시
나타나는 고정 문자열.

### 휴리스틱 경로 (heuristic path)
무엇이 나올지 모르는 채 **모양만 보고 추측하는** 탐지 경로. 확신할 수 없으므로
자동으로 펼치지 않고 사용자의 확인을 거친다.

### 신뢰도 (confidence)
탐지 결과를 자동으로 펼칠지, 표시만 하고 기다릴지를 가르는 기준. 앵커 경로는 항상
고신뢰이고, 휴리스틱 경로는 검사를 얼마나 통과했는지에 따라 갈린다.

### 중첩 깊이 (nesting depth)
하나의 원문 토큰에 인코딩이 몇 겹으로 적용되었는지. 깊이 3은 세 번 연속 인코딩되어
세 번 풀어야 읽을 수 있는 상태를 뜻한다.

### 호스트 블랙리스트 (host blacklist)
사용자가 동작을 끈 사이트 목록. 기본값은 모든 사이트에서 동작하는 것이며,
블랙리스트는 그 예외다.

### 배지 (badge)
휴리스틱 경로가 찾은 후보임을 알리는 작은 표식. 디코딩 블록과 달리 결과를 보여주지
않으며, 사용자가 눌러야 블록이 생긴다. "여기 뭔가 있을지도 모른다"는 말이지
"이것은 base64다"라는 단언이 아니다.

### 케이스 페이지 (case page)
탐지가 어려운 상황을 한 장에 모아둔 검증용 문서. 실제 사이트에서는 재현하기 어렵거나
우연에 기대야 하는 조건들을 의도적으로 만들어 둔다.
